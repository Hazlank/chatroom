import { userlist, messagecontent } from './userList'

const state = {
  theme: 'light',
  barPosition: false,
  wrapper: false,
  box: false,
  speekingNum: '',
  userList: userlist,
  messagecontent: messagecontent,
  boxType: '',
  searchDelete: false
}

export default state
