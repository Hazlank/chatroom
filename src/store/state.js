import { userlist, messagecontent } from './userList'

const state = {
  theme: 'light',
  barPosition: false,
  wrapper: false,
  box: false,
  speekingNum: '',
  userList: userlist,
  messagecontent: messagecontent,
  boxType: ''
}

export default state
