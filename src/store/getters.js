const getters = {
  theme: state => {
    return state.theme
  },
  barPosition: state => {
    return state.barPosition
  },
  wrapper: state => {
    return state.wrapper
  }
}

export default getters
